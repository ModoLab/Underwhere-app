<div class="container mt-3">
  <div class="row d-flex justify-content-center ">
    <div class="col-8 d-flex flex-column justify-content-center card border bg-white px-3 py-3 shadow">

      <h2 class="text-center mb-3">Bienvenue sur UnderWhere</h2>
      <h4 class="mb-3">Connexion :</h4>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                label: "Mot de passe",
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, label: "Rester connecté", as: :boolean if devise_mapping.rememberable?  %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Se connecter", class: "btn btn-primary py-2 px-4 me-3 mb-3" %>
  </div>
<% end %>

<div class="col-8">
  <%= render "devise/shared/links" %>
</div>




    </div>
  </div>
</div>
